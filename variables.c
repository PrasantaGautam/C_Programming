#include <stdio.h>

int main() {
    
    // dataType variableName = value;
    int num = 1; // Here num is variable 
    float num1 = 2; // Here num1 is variable
    // variable is the name to the memory location having data which is initialized on it
    // On -> ( int num = 1 ) value 1 is initialized to the memory location having name num
    
    return 0;
}
