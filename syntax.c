#include <stdio.h>
// The above line is used for including the stdio.h file in the program


int main() {
    
    printf("Hello world"); // printf used to print something on console

    return 0; // using return because int is return type
}
