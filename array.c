#include <stdio.h>

int main() {
  // Datas are stored in array using comma separation
  // int array_name[] = {datas)};
  int numCollec[] = {25, 50, 75, 100};
  
  // We can print specific data using index number inside square bracket
  printf("%d", numCollec[0]);
 
  return 0;
}
